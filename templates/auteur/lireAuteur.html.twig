{% extends 'layout.html.twig' %}

{% block title %}
	{{ parent() }}
	- auteur
{% endblock %}

{% block content %}
	<div class="row mt-5 justify-content-center text-center">
		<h1>{{auteur.nom ~' ' ~ auteur.prenom}}</h1>
		<div>
			<h6 class="text-muted">date de naissance :
				{{auteur.dateDeNaissance | date("d/m/y \à H\\hi")}}</h6>
			<h6>auteur de l'auteur</h6>
			<p>{{auteur.biographie}}
			</p>
			<div>
				<a href="{{ path('update-auteur',{id:auteur.id})}}">
					<i class="fas fa-edit mx-1 text-success"></i>
				</a>
				<a href="{{ path('delete-auteur',{id:auteur.id})}}">
					<i class="fas fa-trash mx-1 text-danger" onclick="return confirm('Etes vous sûr de vouloir supprimer cet auteur?')"></i>
				</a>
			</div>
		</div>
	</div>
{% endblock %}
